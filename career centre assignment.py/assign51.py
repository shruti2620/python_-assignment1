# write python pogram to map two list into dictionary.

keys=['a','b','c','d']
values=[400,400,400,300]

map_dict=dict(zip(keys,values))
print(map_dict)

# zip function pairs each element of key to each element of value.